// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',      // habilita next export
  // opcional: si quieres rutas sin extensión .html
  trailingSlash: false,
};
module.exports = nextConfig;
